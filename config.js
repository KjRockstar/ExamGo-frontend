// ExamGo frontend config
//
// Point this at your deployed backend. While developing locally with
// `npm run dev` in examgo-backend, leave it as localhost. Once you deploy
// to Render, change it to your Render URL, e.g.:
//   const API_BASE_URL = 'https://examgo-backend.onrender.com';

const API_BASE_URL = 'http://localhost:3001';
